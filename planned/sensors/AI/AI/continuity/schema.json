---

## `continuity/schema.json`
```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BioGrid Continuity Capsule",
  "type": "object",
  "required": [
    "continuity_index",
    "project_focus",
    "active_axis",
    "core_glyphs",
    "last_resonance",
    "geometry_encoding",
    "dock_instruction",
    "timestamp_utc"
  ],
  "properties": {
    "continuity_index": {
      "type": "string",
      "pattern": "^C\\d{3,}$",
      "description": "Monotonic capsule index, e.g., C001, C002."
    },
    "project_focus": { "type": "string", "minLength": 1 },
    "active_axis": {
      "type": "array",
      "minItems": 2,
      "maxItems": 2,
      "items": { "type": "string", "minLength": 1 },
      "description": "Exactly two active axes."
    },
    "core_glyphs": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "last_resonance": { "type": "string", "minLength": 1 },
    "geometry_encoding": {
      "type": "object",
      "required": ["shape", "overlay", "placement"],
      "properties": {
        "shape": { "type": "string", "minLength": 1 },
        "overlay": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "placement": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": true
    },
    "dock_instruction": { "type": "string", "minLength": 1 },
    "timestamp_utc": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 UTC timestamp, e.g., 2025-09-03T00:00:00Z"
    },
    "notes": { "type": "string" },
    "meta": { "type": "object", "additionalProperties": true }
  },
  "additionalProperties": false
}
